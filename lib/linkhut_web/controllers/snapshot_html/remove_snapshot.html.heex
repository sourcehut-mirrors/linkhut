<div>
  <h3>Remove snapshot</h3>
  <div class="bookmark">
    <LinkhutWeb.LinkComponents.bookmark_header title={@link.title} url={@link.url} />
  </div>
  <table class="details-table">
    <tbody>
      <tr>
        <th>Format</th>
        <td>{format_display_name(@snapshot.format)}</td>
      </tr>
      <tr>
        <th>Source</th>
        <td>{source_display_name(@snapshot.source)}</td>
      </tr>
      <tr>
        <th>Captured</th>
        <td>{LinkhutWeb.Helpers.format_date_absolute(@snapshot.inserted_at, @preferences.timezone)}</td>
      </tr>
      <tr :if={@snapshot.file_size_bytes}>
        <th>Size</th>
        <td>{format_bytes(@snapshot.file_size_bytes)}</td>
      </tr>
    </tbody>
  </table>
  <hr />
  <.form :let={f} for={%{}} as={:snapshot} action={~p"/_/archive/#{@link.id}/snapshot/#{@snapshot.id}"} method="delete">
    <fieldset>
      <.input field={f[:are_you_sure?]} label="Are you sure?" type="checkbox" />
      <.button type="submit">Delete</.button>
    </fieldset>
  </.form>
</div>
